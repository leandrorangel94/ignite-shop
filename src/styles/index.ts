import { createStitches } from "@stitches/react";

export const {
  config,
  styled,
  css,
  globalCss,
  theme,
  createTheme,
  keyframes,
  getCssText,
} = createStitches({
  theme: {
    colors: {
      rocketseat: "#8257e6",
    },
  },
});
